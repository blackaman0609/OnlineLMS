In backend, create .env file and add your data:
PORT = hidden
MONGODB_URL = "hidden"
JWT_SECRET = "hidden"
USER_EMAIL = "hidden"
USER_PASSWORD = "hidden"

In frontend, create .env file and add your data:
VITE_FIREBASE_APIKEY = "hidden"
